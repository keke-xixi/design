---
description: 用户只发「1」时：为当前改动写好注释、git commit，并推送到 origin
alwaysApply: true
---

# Mode-1 触发

若用户消息去掉首尾空白后**恰好等于** `1`：

1. 立刻阅读并完整执行项目 Skill：`.cursor/skills/tongxuan-mode-1/SKILL.md`
2. 为**当前未提交改动**补上必要注释，然后按仓库规范 `git commit`
3. 提交成功后 **push** 到 `origin`（当前分支）
4. **不要**提问、不要要确认
5. 无改动则说明即可，不要空提交；若仅本地领先远程，仍可只 push

这是聊天触发：在本对话单独发送 `1` 即可。
